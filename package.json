{
  "name": "document-manager",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "istanbul": "istanbul cover node_modules/.bin/_mocha --reporter lcovonly -- -- test/*",
    "seed": "NODE_ENV=test node app/seeders/seed.js",
    "db-setup": "dropdb --if-exists test_doc_manager && createdb test_doc_manager",
    "test": "NODE_ENV=test npm run db-setup && npm run seed && npm run istanbul",
    "start": "NODE_ENV=test npm run seed && nodemon server.js",
    "coverage": "rm -rf coverage/",
    "coveralls": "cat coverage/lcov.info | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cyrielo/document-manager.git"
  },
  "author": "Ologho Cyril Paul",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cyrielo/document-manager/issues"
  },

  "homepage": "https://github.com/cyrielo/document-manager#readme",
  
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.15",
    "dotenv": "^2.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.1.2",
    "supertest": "^2.0.1"
  }
}
